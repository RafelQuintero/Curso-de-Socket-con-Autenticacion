const Categoria = require('./categoria');
const ChatMensajes = require('./chat-mensajes'); //es de otro color ya que me esta indicando que es un clase
const Producto = require('./producto');
const Role = require('./role');
const Server = require('./server'); //es de otro color ya que me esta indicando que es un clase
const Usuario = require('./usuario');

module.exports = {
	Categoria,
	ChatMensajes,
	Producto,
	Role,
	Server,
	Usuario,
};
